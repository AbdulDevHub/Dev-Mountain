# UI Registry

Living document. Updated after every component is built. Read this before building any new component — match existing patterns exactly before inventing new ones.

---

## How to Use

Before building any component:
1. Check if a similar component already exists here.
2. If yes — match its exact classes and design tokens.
3. If no — build it following [ui-rules.md](file:///context/ui-rules.md) and [ui-tokens.md](file:///context/ui-tokens.md), then add it here.

After building any component — update this file with the component name, file path, and exact classes used.

---

## Components

*(No components registered yet. Start adding components as they are built!)*

### Example Component (Structure)

File: `components/ExampleComponent.tsx`
Last updated: YYYY-MM-DD

| Property | Class |
| --- | --- |
| Background | `bg-surface` |
| Border | `border border-border` |
| Border radius | `rounded-md` |
| Text | `text-text-primary text-sm` |
| Spacing | `p-4 m-2` |
| Hover state | `hover:bg-surface-secondary` |
| Shadow | `shadow-sm` |
| Accent usage | `text-accent` |

**Pattern notes:**
*(Notes on usage guidelines, responsibilities, or behaviors)*
